name = "jap-api"
main = "worker.js"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# Variables d'environnement (secrets à ajouter via : wrangler secret put DEEPSEEK_API_KEY)
# [vars]
# DEEPSEEK_API_KEY = ""  # Ne pas mettre la clé ici ! Utiliser wrangler secret
